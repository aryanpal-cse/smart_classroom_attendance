# Face Recognition Section Patch

Extract this ZIP directly into the root of the `smart_classroom_attendance` project and replace existing files.

The patch adds:
- Student Face Recognition Center
- Student dashboard Face Recognition button
- Admin face-registration status page
- Route checks and automated page tests

It does not contain or replace:
- `instance/attendance.db`
- `face_data/`
- `reports/`
- `venv/`
- `.git/`
